<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.db.MemberMapper">

	<insert id="join" parameterType="com.smhrd.model.MemberVO">
		INSERT INTO tb_member(memId, memPw, name, gender, birth) VALUES(#{memId}, #{memPw}, #{name}, #{gender}, #{birth})
	</insert>
	
	<select id="login" parameterType="com.smhrd.model.MemberVO" resultType="com.smhrd.model.MemberVO">
		SELECT memId, memPw, name From tb_member where memId = #{memId} AND memPw = #{memPw}
	</select>
	
	<update id="update" parameterType="com.smhrd.model.MemberVO">
		UPDATE tb_member set memPw = #{memPw} Where memId = #{memId}
	</update> 
	
	<delete id="delete">
		DELETE FROM tb_member WHERE memId = #{memId}
	</delete>
</mapper>