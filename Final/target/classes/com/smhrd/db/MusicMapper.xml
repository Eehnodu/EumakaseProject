<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.db.MusicMapper">

	<select id="getMusic" parameterType="com.smhrd.model.MusicVO" resultType="com.smhrd.model.MusicVO">
		SELECT * FROM tb_music WHERE artist LIKE CONCAT('%', #{artist}, '%') AND title LIKE CONCAT('%', #{title}, '%') LIMIT 1
	</select>

	<!-- other의 앨범커버 가져오기 -->
	<select id="getOtherAlbumCov" parameterType="int" resultType="com.smhrd.model.MusicVO">
			SELECT albumCov FROM tb_music WHERE musicIdx = #{musicIdx}
	</select>
	
</mapper>